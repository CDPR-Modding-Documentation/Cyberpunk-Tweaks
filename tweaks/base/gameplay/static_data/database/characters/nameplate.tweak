package NameplateConfig

FrustumRange
{
	float distance = 1.f;
	Vector2 min = ( -25.5f, -40.f );
	Vector2 max = ( 25.5f, 40.f );
}

FrustumRangeClosest : FrustumRange
{
	float distance = 0.01f;
	Vector2 min = ( -10.f, -10.f );
	Vector2 max = ( 10.f, 20.f );
}

FrustumRangeFurthest : FrustumRange
{
	float distance = 20.f;
	Vector2 min = ( -3.f, -3.f );
	Vector2 max = ( 3.f, 10.f );
}

float showTimeout = 0.0f;
float hideTimeout = 1.0f;
float transitionTimeout = 0.05f;
